# Cross-Agent or Checkpoint Handoff: <change-id> / <task-id>

- Working record and approval evidence:
- Task boundary and result:
- Files/symbols changed:
- Container commands and exits:
- Tests not run and consequence:
- Deviations or new risks:
- Pending items captured:
- Next checkpoint requiring human direction:

Do not create this separate handoff for routine same-context tasks; append a concise entry to `CHANGE_WORKING.md` instead.
